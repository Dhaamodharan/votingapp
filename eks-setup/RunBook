Provision resources using terraform
Once the code is ready, run the below commands(Ensure terraform is installed)

    Run terraform init to initialize the configuration.
    Run terraform plan to review the resources that will be created.
    Run terraform apply to create the resources.

Once the cluster is online, update the kubeconfig file to interact with the EKS cluster using kubectl.

    aws eks --region us-west-2 update-kubeconfig --name energyweb-eks-cluster
